spring:
  profiles:
    active: dev
  thymeleaf:
    mode: HTML
  devtools:
    restart:
      trigger-file: .trigger
book:
    name: networking world
    author: shao
    description: ${book:name}, this book is great